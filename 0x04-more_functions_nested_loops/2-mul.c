#include "main.h"

/**
 * mul - function checks for checks for a digit 0 through.
 * @a: a - Variables
 * @b: b - Variables
 * Return: Always 0
 */

int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
